import { OnlyPositiveDirective } from './only-positive.directive';

describe('OnlyPositiveDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyPositiveDirective();
    expect(directive).toBeTruthy();
  });
});
